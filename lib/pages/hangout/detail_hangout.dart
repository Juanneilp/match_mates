import 'package:flutter/material.dart';

class HangoutDetailPage extends StatelessWidget {
  const HangoutDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Detail Page Hangout'),
      ),
    );
  }
}
