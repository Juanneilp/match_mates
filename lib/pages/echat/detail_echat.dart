import 'package:flutter/material.dart';

class EChatDetailPage extends StatelessWidget {
  const EChatDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Detail Page E-Chat'
        ),
      ),
    );
  }
}
